/**
 * Prerendered shell reading the house record client-side, /menu/preps's
 * pattern. The producers live in IndexedDB; the static build ships only the
 * frame, and there is nothing from the guide to load for it.
 */
export const prerender = true;
