// Prerendered shell reading the session store client-side — the /menu page's
// own pattern. The dishes live in IndexedDB; the static build ships an empty
// frame that hydrates into the quiz.
export const prerender = true;
